// You know the rules.
import { DEFAULT_VALUE } from '@/constants';

const state = {
  qrValue: DEFAULT_VALUE,
  qrLightColor: '#ffffff',
  qrDarkColor: '#000000',
  fileType: 'png',
};

export default state;
